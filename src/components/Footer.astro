---
const year = new Date().getFullYear();
---

<footer>
	<span>&copy; {year} abdi</span>
</footer>

<style>
	footer {
		max-width: var(--page-width);
		margin: 0 auto;
		padding: var(--space-8) var(--page-pad);
		text-align: center;
		font-size: 0.8rem;
		color: var(--text-muted);
		border-top: 1px solid var(--line);
	}
</style>
